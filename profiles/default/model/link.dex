
import model.node;

class Link
{
public:
  json::Json url;
  json::Json text;

public:
  Link(const json::Json & u, const json::Json & t)
    : url(u), text(t)
  {
  }

  Link() = default;
  Link(const Link&) = default;

  ~Link() = default;
};
