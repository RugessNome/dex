
/*!
 * \class Bar
 * \brief The Bar class.
 * This is an example file 
 * with \b big text.
 *
 * This class is related to \t Foo.
 * 
 * This {is a group}.
 *
 * \begin[cpp]{code}
 * int a = 5;
 * if(a > 5)
 *   print("Hell no!");
 * \end{code}
 *
 * I also wanted to link to \l {http://www.github.com} GitHub.
 *
 * The main methods are \c foo and \c{bar()}.
 *
 * Main methods:
 * \begin{list}
 *   \li \c foo
 *   \li also there is \b bar.
 * \end{list}
 *
 * \input{input.txt}
 *
 * This is some text after an input command.
 */

 
  
/*!
 * \fun int foo();
 * \brief A test function.
 * \returns 5 
 */
 
/*!
 * \fun int add(int a, int b);
 * \param The first integer to add
 * \param The second integer to add 
 * \brief Adds two integers.
 */

 /*!
   \fun int mul(int a, int b);
   \brief Multiplies two integers.
   \begin[cpp]{code}
     int a = 5;
     if(a > 5)
       print("Hell no!");
   \end{code}
 */